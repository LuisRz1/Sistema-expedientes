{
    "$schema": "https://schema.railpack.com",
    "steps": {
        "extensions": {
            "commands": [
                "install-php-extensions ctype curl dom fileinfo filter gd hash mbstring openssl pcre pdo session tokenizer xml pdo_mysql redis"
            ]
        }
    }
}
